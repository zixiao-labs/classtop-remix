{
  "name": "classtop-remix",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "dev:electron": "vite build && npx esbuild electron/main.ts --bundle --platform=node --outdir=dist-electron --format=esm --external:electron --external:better-sqlite3 && npx esbuild electron/preload.ts --bundle --platform=node --outdir=dist-electron --format=cjs --external:electron && electron .",
    "build:electron": "vite build && npx esbuild electron/main.ts --bundle --platform=node --outdir=dist-electron --format=esm --external:electron --external:better-sqlite3 && npx esbuild electron/preload.ts --bundle --platform=node --outdir=dist-electron --format=cjs --external:electron && electron-builder",
    "postinstall": "electron-rebuild"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "chen-the-dawnstreak": "^2.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router": "^7.13.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "electron": "^40.0.0",
    "electron-builder": "^26.0.0",
    "electron-rebuild": "^3.2.9",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
